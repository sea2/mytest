package com.example.test.mytestdemo.adapter.base;

public class SampleActivity {
 /*     // 设置适配器
        lvtest.setAdapter(mCommonAdapter = new CommonAdapter<UserInfo>(getApplicationContext(), mDatas, R.layout.item_list) {
            @Override
            public void convert(ViewHolder helper, UserInfo item) {
                if (item != null) {
                    helper.setText(R.id.tv_left, item.getId() + "--");
                    TextView tv = helper.getView(R.id.tv_right);
                    tv.setText(item.getName());
          //              helper.getView(R.id.tv_title).setOnClickListener(l)
                }
            }
        });
        */
}
